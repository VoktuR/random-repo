create table products (
                          id                      bigserial primary key,
                          title                   varchar(100),
                          category                varchar(100),
                          price                   bigint
);